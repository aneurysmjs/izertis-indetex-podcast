import { FC, StrictMode } from 'react';

import './assets/css/styles.css';

import Routing from '@/routing';

const App: FC = () => (
  <StrictMode>
    <Routing />
  </StrictMode>
);

export default App;
